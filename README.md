# jangalinski.github.io

Personal GitHub Pages site rebuilt with Gradle, Kotlin, and Kobweb.

## Structure

* `site/` contains the Kobweb site.
* `app/` is the remaining Gradle/Kotlin sample module from the init setup.

## Notes

The revived site keeps the old blog content in markdown form and reuses the original Clean Blog theme assets.
